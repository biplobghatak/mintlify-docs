---
title: "How Outpulse works"
description: "The core concepts behind monitors, signals, drafts, and the posting pipeline."
---

## Core concepts

Understanding these five concepts gives you a complete mental model of Outpulse.

### Monitor

A monitor is a saved search rule. It defines *what* Outpulse looks for on X — a keyword phrase, a hashtag, a competitor's replies, or any custom query.

Outpulse runs every active monitor every 20 minutes. A single workspace can have unlimited monitors.

→ [Create and manage monitors](/core/monitors)

### Signal

A signal is a post that matched a monitor *and* passed the intent threshold. Not every matching post becomes a signal — Outpulse scores each one first.

Signals appear in your **Inbox** with an intent score (1–10), the author's profile, and the reason it matched your ICP. Signals with a score below your threshold are discarded silently.

→ [Working the inbox](/core/inbox)

### Draft

A draft is an AI-generated reply or DM for a signal. When you click "Generate reply" or "Generate DM" on a signal, Outpulse sends the post, your ICP, and your tone profile to Claude. The result is a draft in your writing style, ready to approve or edit.

You can generate two variants at once for comparison, or regenerate with feedback if the first draft misses the mark.

→ [Drafts and AI writing](/core/drafts)

### Queue

Approved drafts go into the posting queue. Outpulse doesn't post instantly — it waits a human-like delay (45–90 seconds), applies your schedule constraints (e.g. only post Mon–Fri, 9am–6pm), and enforces daily posting caps per action type.

The queue shows pending, posted, and failed actions so you always know what's happening.

→ [Managing the queue](/core/queue)

### Credit

Credits are the unit of usage. Outpulse charges credits for the actions that cost compute or API calls — finding leads, generating drafts, and posting to X. Credits never expire and you pay only for what you use.

→ [How credits work](/credits/how-credits-work)

---

## The automation loop

```
Every 20 minutes per active monitor:

  Monitor query
       ↓
  Fetch matching posts (TwitterAPI.io)
       ↓
  Deduplicate (48h Redis window)
       ↓
  Score each post for intent 1–10 (Claude)
       ↓
  Score ≥ threshold?
     Yes → Save as Signal, charge 2 credits
     No  → Discard
       ↓
  Automation mode?
     Manual     → Signal waits in inbox for your action
     Smart auto → AI generates draft and decides whether to post
     Full auto  → Draft generated and queued immediately
       ↓
  Posting queue
     Human-like delay (45–90s)
     Schedule constraints (hours, days, timezone)
     Daily caps enforced
       ↓
  Post to X via Twitter API v2
  Charge 2 credits per successful post
```

---

## Posting modes

Each monitor has its own automation mode. You can run different monitors at different automation levels depending on how much you trust the context.

| Mode | What happens |
|------|-------------|
| **Manual** | Signals appear in inbox. You generate drafts and approve each one. |
| **Smart auto** | Outpulse generates drafts automatically. AI decides which ones are safe to post. You review the rest. |
| **Full auto** | Every qualifying signal gets a draft generated and posted without your input. |

→ [Posting modes in detail](/core/posting-modes)

---

## Account safety

Outpulse is built to protect your X account from being flagged or banned.

- **Human-like delays** between every action (45–90s + random jitter)
- **Daily hard caps** on replies, DMs, and tweets
- **Content similarity check** before every post — never sends duplicate messages
- **Warm-up mode** for new or small accounts — starts with lower caps that increase gradually
- **Rate limiting** per connected X account

These safeguards run automatically. You don't need to configure them.
